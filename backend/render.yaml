services:
  - type: web
    name: pricedex-backend
    env: python
    buildCommand: pip install -r requirements.txt && python manage.py migrate && python manage.py loaddata seed.json || true && python manage.py createsuperuser --noinput
    startCommand: gunicorn core.wsgi:application
